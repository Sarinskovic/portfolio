---
const { title, index, logo, description, path } = Astro.props;
---

<div
  class={`flex ${index % 2 === 0 ? "flex-row" : "flex-row-reverse"} gap-4 border-b-[1px] border-b-cyan-700 justify-between min-h-96 p-4 text-cyan-50`}
>
  <div class={`basis-1/2 flex justify-center flex-col gap-4 items-center`}>
    <div class="flex flex-col gap-4">
      <h1 class="max-w-96 font-bold text-2xl sm:text-3xl text-orange-400">
        {title}
      </h1>
      <p class={`max-w-96 text-sm text-balance sm:text-lg`}>
        {description}
      </p>
      <a
        href={`/portfolio/company/${path}`}
        class="text-xs underline-offset-8 underline sm:text-lg text-sunshade-300 hover:decoration-wavy"
      >
        Scopri di più
      </a>
    </div>
  </div>
  <div class="basis-1/2 flex justify-center items-center object-contain">
    <img class="w-full h-96" src={logo} alt={logo} />
  </div>
</div>
